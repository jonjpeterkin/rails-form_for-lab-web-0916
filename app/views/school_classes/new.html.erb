<%= form_for @school_class do |f| %>
	School Class Form <br>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<%= f.label :room_number %>
	<%= f.text_field :room_number %>
	<%= f.submit "Create School class"%>
<% end %>